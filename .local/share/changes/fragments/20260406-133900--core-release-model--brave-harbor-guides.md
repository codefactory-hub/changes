+++
type = "added"
public_api = "add"
behavior = "new"
+++

Introduce the core `changes` release model for Git repositories.

This adds repo-local configuration, durable fragment storage, canonical release records, and explicit `status` / `release` workflows for assembling and recording releases from pending fragments.
