+++
type = "changed"
bump = "major"
breaking = true
+++

Rename the fragment authoring command to `create` and add guided authoring.

Prompt for type, optional name stem, and body text in TTY sessions, add `--edit` for scaffolded editor-driven drafting, and include name stems in generated fragment filenames.
