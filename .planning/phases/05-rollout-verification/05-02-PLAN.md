---
phase: 05-rollout-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - internal/app/doctor_test.go
  - internal/cli/app_integration_test.go
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The rollout boundary is documented clearly: manifest-backed repos operate normally, while legacy repos require doctor-guided repair or migration."
    - "The layered rollout matrix covers authority failures, migration-prompt generation, and the focused precedence set without regressing the full repository."
    - "Phase 5 closes with explicit verification artifacts rather than relying on implicit confidence from earlier phases."
  artifacts:
    - path: internal/app/doctor_test.go
      provides: Migration-prompt and legacy-diagnosability regression coverage in rollout scenarios
    - path: internal/cli/app_integration_test.go
      provides: Operator-facing rollout verification for default behavior, legacy failures, and doctor guidance
    - path: README.md
      provides: Explicit rollout boundary documentation for legacy repos and doctor-guided repair/migration
  key_links:
    - from: README.md
      to: internal/cli/app.go
      via: documented rollout boundary and operator commands
      pattern: "changes doctor|changes init|legacy|layout.toml"
    - from: internal/cli/app_integration_test.go
      to: internal/app/doctor.go
      via: rollout diagnostics and migration brief behavior
      pattern: "doctor|migration-prompt|legacy"
---

<objective>
Finish the rollout-verification phase by documenting the rollout boundary and proving the layered verification matrix against the real CLI and app behavior.

Purpose: Turn the Phase 5 verification stance into explicit operator guidance and final proof that the repository-wide test surface still holds.
Output: Final rollout docs adjustments if needed, layered verification tests, and a clean phase verification result.
</objective>

<execution_context>
@/Users/tim.shadel/.local/share/codex/get-shit-done/workflows/execute-plan.md
@/Users/tim.shadel/.local/share/codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-rollout-verification/05-CONTEXT.md
@.planning/phases/05-rollout-verification/05-01-PLAN.md
@.planning/phases/04-command-ux-and-migration-help/04-VERIFICATION.md
@README.md
@internal/app/doctor.go
@internal/cli/app.go
@internal/cli/app_integration_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the rollout boundary for legacy repositories</name>
  <files>README.md</files>
  <read_first>.planning/phases/05-rollout-verification/05-CONTEXT.md
.planning/phases/04-command-ux-and-migration-help/04-VERIFICATION.md
README.md
internal/cli/app.go</read_first>
  <action>Adjust `README.md` only as needed so the rollout boundary is explicit for operators: manifest-backed repos operate normally, while older repos without `layout.toml` should be inspected and repaired or migrated through `changes doctor` rather than expected to continue working silently. Keep the documentation brief and consistent with the locked Phase 4 command surface. Do not rewrite the layout docs structure; this task is about making the legacy rollout stance explicit and discoverable.</action>
  <verify>
    <automated>rg -n 'layout.toml|legacy|changes doctor --scope repo --explain|changes doctor --migration-prompt --scope repo --to home' README.md</automated>
  </verify>
  <acceptance_criteria>
    - The README makes the manifest-backed rollout boundary explicit.
    - The README points legacy repos toward `doctor` rather than implying silent compatibility.
    - The automated verification command exits with status 0.
  </acceptance_criteria>
  <done>The operator docs now explain the intended rollout stance for older repos without reopening compatibility policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the layered rollout matrix and lock final regression coverage</name>
  <files>internal/app/doctor_test.go
internal/cli/app_integration_test.go</files>
  <read_first>.planning/phases/05-rollout-verification/05-CONTEXT.md
.planning/phases/05-rollout-verification/05-01-PLAN.md
internal/app/doctor_test.go
internal/cli/app_integration_test.go</read_first>
  <behavior>
    - Test `TestDoctorExplainsLegacyRepoRepairPath` proves doctor output remains useful in the legacy rollout scenario.
    - Test `TestCLIWarnsOrFailsConsistentlyAcrossFocusedRolloutScenarios` proves the CLI surfaces the intended defaults, terse failures, and doctor guidance across the focused rollout set.
  </behavior>
  <action>Add any remaining app or CLI rollout checks needed to complete the layered matrix from Phase 5 context, then run the final regression commands for the phase. Keep this plan focused on verification closure rather than new feature behavior. The goal is to ensure the focused precedence set, mixed compatibility stance, migration-prompt path, and authority behaviors all survive together under the real test suite.</action>
  <verify>
    <automated>go test ./internal/config ./internal/app ./internal/cli -count=1</automated>
    <automated>go test ./... </automated>
  </verify>
  <acceptance_criteria>
    - The layered rollout matrix is represented in app and CLI regression coverage.
    - The package-level verification pass exits with status 0.
    - The full repository test suite exits with status 0.
  </acceptance_criteria>
  <done>Phase 5 closes with explicit layered verification evidence instead of relying only on feature-level tests from earlier phases.</done>
</task>

</tasks>

<verification>
Run the task-level verification commands, then use the full `go test ./...` regression pass as the final phase-closeout check. Use the plain Go command first, adding a writable cache override only if the environment actually requires it.
</verification>

<success_criteria>
Plan 05-02 is complete when the legacy rollout boundary is documented, the layered rollout matrix is covered in app/CLI regression tests, and the full Go test suite passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rollout-verification/05-rollout-verification-02-SUMMARY.md`
</output>
