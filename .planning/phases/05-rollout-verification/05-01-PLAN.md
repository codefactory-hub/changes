---
phase: 05-rollout-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/init_defaults.go
  - internal/config/init_defaults_test.go
  - internal/config/resolution_test.go
  - internal/app/init_test.go
  - internal/app/doctor_test.go
  - internal/cli/app_integration_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Plain `changes init` defaults to repo `xdg`, and plain `changes init global` defaults to global `xdg`."
    - "Repo init selection follows the approved focused precedence set: explicit flags, `[repo.init]` global defaults, `CHANGES_HOME` repo-style signal, XDG env signal, then built-in default locations."
    - "Manifest-backed XDG and `home` repositories continue to operate without manual migration."
    - "Legacy repos without manifests fail cleanly for ordinary commands and remain diagnosable through `doctor`."
  artifacts:
    - path: internal/config/init_defaults_test.go
      provides: Focused precedence coverage for repo and global init defaults
    - path: internal/app/init_test.go
      provides: Manifest-backed repo/global init compatibility and legacy failure regression coverage
    - path: internal/app/doctor_test.go
      provides: Legacy-only diagnosability checks for rollout scenarios
    - path: internal/cli/app_integration_test.go
      provides: Operator-facing rollout checks for defaults, legacy failures, and doctor diagnosability
  key_links:
    - from: internal/config/init_defaults.go
      to: internal/config/init_defaults_test.go
      via: focused precedence selection behavior
      pattern: "SelectRepoInitLayout|SelectGlobalInitLayout|CHANGES_HOME|repo.init"
    - from: internal/app/init_test.go
      to: internal/config/resolution.go
      via: manifest-backed compatibility and legacy-only failure scenarios
      pattern: "layout.toml|legacy|authority"
    - from: internal/cli/app_integration_test.go
      to: internal/app/doctor.go
      via: legacy rollout diagnostics through doctor
      pattern: "doctor|legacy|migration-prompt"
---

<objective>
Lock the default-safe rollout behavior by proving the approved precedence rules and compatibility boundaries across repo and global initialization.

Purpose: Ensure the shipped defaults match the approved model and that manifest-backed repos keep working while legacy repos fail in the intended diagnosable way.
Output: Focused precedence and compatibility tests, plus any small behavior alignments required to make the rollout model exact.
</objective>

<execution_context>
@/Users/tim.shadel/.local/share/codex/get-shit-done/workflows/execute-plan.md
@/Users/tim.shadel/.local/share/codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/proposals/layout-resolution.md
@.planning/phases/05-rollout-verification/05-CONTEXT.md
@.planning/phases/04-command-ux-and-migration-help/04-VERIFICATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@internal/config/init_defaults.go
@internal/config/resolution.go
@internal/app/init.go
@internal/app/doctor.go
@internal/cli/app.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the focused default-selection precedence set</name>
  <files>internal/config/init_defaults.go
internal/config/init_defaults_test.go
internal/app/init_test.go
internal/cli/app_integration_test.go</files>
  <read_first>.planning/proposals/layout-resolution.md
.planning/phases/05-rollout-verification/05-CONTEXT.md
internal/config/init_defaults.go
internal/config/init_defaults_test.go
internal/app/init.go
internal/app/init_test.go
internal/cli/app_integration_test.go</read_first>
  <behavior>
    - Test `TestSelectRepoInitLayoutUsesGlobalRepoInitDefaultsBeforeEnvSignals` proves repo init honors manifest-backed `[repo.init]` defaults before `CHANGES_HOME` and XDG env signals.
    - Test `TestSelectRepoInitLayoutExplicitFlagsBeatGlobalDefaultsAndEnvSignals` proves explicit repo-init flags win over every other default source.
    - Test `TestSelectGlobalInitLayoutPrefersChangesHomeOverXDGEnv` proves global init prefers `CHANGES_HOME` over XDG env inputs.
    - Test `TestInitializeDefaultsToRepoXDGWithoutOverrides` proves plain repo init selects repo `xdg` and reports the XDG paths.
    - Test `TestInitializeGlobalDefaultsToXDGWithoutOverrides` proves plain global init selects global `xdg`.
    - Test `TestAppInitUsesGlobalRepoInitDefaultsWhenPresent` proves the CLI rollout path respects `[repo.init]` defaults when they are manifest-backed and authoritative.
  </behavior>
  <action>Add or refine focused precedence tests so Phase 5 proves the locked rollout order without exploding into an exhaustive matrix. Keep the verification centered on the approved precedence set from Phase 5 context: explicit flags first, then `[repo.init]` global defaults for repo init, then `CHANGES_HOME` / XDG env signals, then built-in default locations. If the tests uncover small misalignments in `internal/config/init_defaults.go` or `internal/app/init.go`, fix only those alignment issues needed to satisfy the approved behavior; do not expand the selection model beyond the locked styles and sources.</action>
  <verify>
    <automated>go test ./internal/config ./internal/app ./internal/cli -run 'TestSelectRepoInitLayoutUsesGlobalRepoInitDefaultsBeforeEnvSignals|TestSelectRepoInitLayoutExplicitFlagsBeatGlobalDefaultsAndEnvSignals|TestSelectGlobalInitLayoutPrefersChangesHomeOverXDGEnv|TestInitializeDefaultsToRepoXDGWithoutOverrides|TestInitializeGlobalDefaultsToXDGWithoutOverrides|TestAppInitUsesGlobalRepoInitDefaultsWhenPresent' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Focused precedence tests exist for repo init defaults, global init defaults, explicit-flag priority, and `CHANGES_HOME` vs XDG env behavior.
    - Plain repo init and plain global init both prove default `xdg` selection.
    - Repo init proves `[repo.init]` defaults outrank environment signals when authoritative global config exists.
    - The automated verification command exits with status 0.
  </acceptance_criteria>
  <done>The approved default-selection model is proven through focused regression coverage without expanding into an exhaustive matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove manifest-backed compatibility and legacy-only rollout behavior</name>
  <files>internal/config/resolution_test.go
internal/app/init_test.go
internal/app/doctor_test.go
internal/cli/app_integration_test.go</files>
  <read_first>.planning/phases/05-rollout-verification/05-CONTEXT.md
.planning/phases/04-command-ux-and-migration-help/04-VERIFICATION.md
internal/config/resolution.go
internal/app/init.go
internal/app/doctor.go
internal/cli/app.go
internal/cli/app_integration_test.go</read_first>
  <behavior>
    - Test `TestManifestBackedXDGRepoOperatesWithoutMigration` proves an existing manifest-backed repo `xdg` layout remains operational for normal commands.
    - Test `TestManifestBackedHomeRepoOperatesWithoutMigration` proves an existing manifest-backed repo `home` layout remains operational for normal commands.
    - Test `TestLegacyRepoFailsCleanlyForOrdinaryCommands` proves a legacy repo without `layout.toml` does not operate normally and fails through the intended authority path.
    - Test `TestDoctorInspectsLegacyRepoWithoutManifest` proves `doctor` still inspects the same legacy repo successfully.
    - Test `TestMigrationPromptStillGeneratesForLegacyRepoScenario` proves the legacy rollout path still has migration guidance available.
  </behavior>
  <action>Add rollout verification for the mixed compatibility stance. Reuse existing resolver, doctor, and CLI helpers to build disk-backed scenarios that distinguish manifest-backed repos from legacy-only repos. Confirm that manifest-backed `xdg` and `home` repos continue to behave normally without manual migration, while legacy repos fail cleanly for ordinary commands but remain inspectable and migration-brief-capable through `doctor`. Keep the tests layered: resolver/app/CLI scenarios rather than one giant end-to-end script.</action>
  <verify>
    <automated>go test ./internal/config ./internal/app ./internal/cli -run 'TestManifestBackedXDGRepoOperatesWithoutMigration|TestManifestBackedHomeRepoOperatesWithoutMigration|TestLegacyRepoFailsCleanlyForOrdinaryCommands|TestDoctorInspectsLegacyRepoWithoutManifest|TestMigrationPromptStillGeneratesForLegacyRepoScenario' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Manifest-backed repo `xdg` and repo `home` compatibility scenarios are covered.
    - Legacy no-manifest repo behavior is covered for both ordinary-command failure and doctor diagnosability.
    - Migration-prompt generation remains available in the legacy rollout scenario.
    - The automated verification command exits with status 0.
  </acceptance_criteria>
  <done>The rollout boundary is explicit in tests: manifest-backed repos operate normally, while legacy repos fail cleanly and remain diagnosable through doctor.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/config ./internal/app ./internal/cli -count=1` after both tasks complete. Use the plain Go command first, adding a writable cache override only if the environment actually requires it.
</verification>

<success_criteria>
Plan 05-01 is complete when the focused precedence set and the mixed compatibility stance are both proven through targeted automated coverage, and any discovered alignment gaps are closed without reopening earlier design decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rollout-verification/05-rollout-verification-01-SUMMARY.md`
</output>
