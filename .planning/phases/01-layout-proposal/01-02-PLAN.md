---
phase: 01-layout-proposal
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - .planning/phases/01-layout-proposal/01-requirement-decision-matrix.md
  - .planning/phases/01-layout-proposal/01-implementation-gate.md
  - .planning/proposals/layout-resolution.md
  - .planning/phases/01-layout-proposal/01-command-contract.md
  - .planning/phases/01-layout-proposal/01-diagnostic-model.md
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03
  - CMD-04
  - CMD-05
must_haves:
  truths:
    - Every Phase 1 requirement and locked decision has one definitive proposal home before implementation starts.
    - The artifact set explicitly locks doctor inspection, migration prompt generation, authoritative selection, manifest validity, config shape, and repo hygiene.
    - Phase 2 can begin without reopening command names, precedence, or migration safety behavior.
  artifacts:
    - path: .planning/phases/01-layout-proposal/01-requirement-decision-matrix.md
      provides: Requirement and decision traceability across the Phase 1 proposal bundle
      contains: "CMD-01"
    - path: .planning/phases/01-layout-proposal/01-implementation-gate.md
      provides: Final pre-implementation gate with pass/fail checks for the approved design
      contains: "Phase 2 entry gate"
    - path: .planning/proposals/layout-resolution.md
      provides: Finalized governing proposal after consistency review
      contains: "## Approved Command Surface"
  key_links:
    - from: .planning/phases/01-layout-proposal/01-requirement-decision-matrix.md
      to: .planning/proposals/layout-resolution.md
      via: Each requirement and decision row names the artifact and exact section that satisfies it
      pattern: "CMD-01|D-01"
    - from: .planning/phases/01-layout-proposal/01-implementation-gate.md
      to: .planning/phases/01-layout-proposal/01-command-contract.md
      via: The gate checklist restates the same command family, flag, and doctor tier rules as pass/fail items
      pattern: "Doctor tiers: default concise, `--explain` rich, `--json` structured"
---

<objective>
Validate the full Phase 1 proposal bundle and lock a no-open-questions implementation gate.

Purpose: Ensure every requirement and user decision is covered by a concrete proposal artifact and reconcile any drift before implementation work begins.
Output: A requirement/decision matrix, a final implementation gate, and any consistency edits needed across the proposal bundle.
</objective>

<execution_context>
@/Users/tim.shadel/.local/share/codex/get-shit-done/workflows/execute-plan.md
@/Users/tim.shadel/.local/share/codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-layout-proposal/01-CONTEXT.md
@.planning/proposals/layout-resolution.md
@.planning/phases/01-layout-proposal/01-command-contract.md
@.planning/phases/01-layout-proposal/01-diagnostic-model.md
@internal/config/config.go
@internal/app/init.go
@internal/cli/app.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the requirement and decision coverage matrix</name>
  <files>.planning/phases/01-layout-proposal/01-requirement-decision-matrix.md</files>
  <read_first>.planning/phases/01-layout-proposal/01-requirement-decision-matrix.md, .planning/phases/01-layout-proposal/01-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/proposals/layout-resolution.md, .planning/phases/01-layout-proposal/01-command-contract.md, .planning/phases/01-layout-proposal/01-diagnostic-model.md</read_first>
  <action>Create `.planning/phases/01-layout-proposal/01-requirement-decision-matrix.md` with the title `# Phase 1 Requirement and Decision Matrix`. Add one table for requirements and one table for locked decisions. In the requirements table, include every row `CMD-01`, `CMD-02`, `CMD-03`, `CMD-04`, and `CMD-05` with columns `Requirement ID`, `Artifact File`, `Exact Section or Command`, and `Status`; mark every status as `Covered`. In the decisions table, include every row `D-01` through `D-24` with columns `Decision ID`, `Artifact File`, `Exact Heading or Rule Text`, and `Status`; mark every status as `Locked`. Add a `## Coverage Notes` section that says `Deferred ideas: none.` and `Open design questions: none.` If any requirement or decision lacks an exact home while building the matrix, update the matrix row to point at the missing artifact location you will fix in Task 2 instead of inventing a partial status; do not introduce `Partial`, `Future`, or `v1` labels.</action>
  <verify>rg -n "CMD-01|CMD-02|CMD-03|CMD-04|CMD-05|D-01|D-12|D-24|Deferred ideas: none\\.|Open design questions: none\\.|Covered|Locked" .planning/phases/01-layout-proposal/01-requirement-decision-matrix.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-layout-proposal/01-requirement-decision-matrix.md` contains every requirement ID `CMD-01`, `CMD-02`, `CMD-03`, `CMD-04`, and `CMD-05`.
    - `.planning/phases/01-layout-proposal/01-requirement-decision-matrix.md` contains every decision ID from `D-01` through `D-24`.
    - `.planning/phases/01-layout-proposal/01-requirement-decision-matrix.md` contains the exact status words `Covered` and `Locked`.
    - `.planning/phases/01-layout-proposal/01-requirement-decision-matrix.md` contains the exact sentences `Deferred ideas: none.` and `Open design questions: none.`
    - `.planning/phases/01-layout-proposal/01-requirement-decision-matrix.md` does not contain the words `Partial`, `Future`, `v1`, or `placeholder`.
  </acceptance_criteria>
  <done>The phase has a single reviewable matrix proving that every Phase 1 requirement and locked decision maps to a concrete proposal artifact before coding begins.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the proposal bundle and publish the implementation gate</name>
  <files>.planning/phases/01-layout-proposal/01-implementation-gate.md, .planning/proposals/layout-resolution.md, .planning/phases/01-layout-proposal/01-command-contract.md, .planning/phases/01-layout-proposal/01-diagnostic-model.md</files>
  <read_first>.planning/phases/01-layout-proposal/01-implementation-gate.md, .planning/phases/01-layout-proposal/01-requirement-decision-matrix.md, .planning/proposals/layout-resolution.md, .planning/phases/01-layout-proposal/01-command-contract.md, .planning/phases/01-layout-proposal/01-diagnostic-model.md, .planning/phases/01-layout-proposal/01-CONTEXT.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, internal/config/config.go, internal/app/init.go, internal/cli/app.go</read_first>
  <action>Create `.planning/phases/01-layout-proposal/01-implementation-gate.md` with the title `# Phase 1 Implementation Gate` and a section `## Phase 2 entry gate`. In that section, add one pass/fail checklist item for each locked rule below, using the exact wording shown here: `Supported styles: xdg and home only.`, `Scopes resolved independently: global and repo.`, `Global bootstrap precedence: flags > CHANGES_HOME > XDG env vars > built-in default locations.`, `Repo init precedence: flags > [repo.init] defaults > CHANGES_HOME signal > XDG env signal > built-in default locations.`, `Operational validity requires parseable layout.toml with matching scope and style.`, `Legacy-only detection is doctor-visible but invalid for ordinary commands.`, `Multiple supported candidates = ambiguity error.`, `Repair or manifest stamping is explicit only.`, `Doctor tiers: default concise, --explain rich, --json structured.`, `Migration prompt is an advisory Markdown brief with required verification and explicit no-dual-write instructions.`, `Global config bootstrap keys are limited to [repo.init].`, and `Repo state ignore rules are /.local/state/ and /.changes/state/.` Add `Open questions: none.` and `Phase 2 may begin only when every checklist item is Pass.` After writing the gate, compare the matrix against `.planning/proposals/layout-resolution.md`, `01-command-contract.md`, and `01-diagnostic-model.md`; if any artifact uses different command names, precedence wording, candidate-state names, config rules, or migration safety language than the gate, edit those files so they all use the same exact values before marking the gate complete. Do not add new options, alternate command families, or looser wording like `usually`, `typically`, or `may choose silently`.</action>
  <verify>rg -n "Phase 2 entry gate|Supported styles: xdg and home only\\.|Scopes resolved independently: global and repo\\.|Global bootstrap precedence: flags > CHANGES_HOME > XDG env vars > built-in default locations\\.|Repo init precedence: flags > \\[repo.init\\] defaults > CHANGES_HOME signal > XDG env signal > built-in default locations\\.|Operational validity requires parseable layout.toml with matching scope and style\\.|Legacy-only detection is doctor-visible but invalid for ordinary commands\\.|Multiple supported candidates = ambiguity error\\.|Repair or manifest stamping is explicit only\\.|Doctor tiers: default concise, --explain rich, --json structured\\.|Migration prompt is an advisory Markdown brief with required verification and explicit no-dual-write instructions\\.|Global config bootstrap keys are limited to \\[repo.init\\]\\.|Repo state ignore rules are /\\.local/state/ and /\\.changes/state/\\.|Open questions: none\\.|Phase 2 may begin only when every checklist item is Pass\\." .planning/phases/01-layout-proposal/01-implementation-gate.md && rg -n "changes init global|changes doctor --migration-prompt|authoritative|ambiguous|legacy-detected|uninitialized|\\[repo.init\\]|dual-write" .planning/proposals/layout-resolution.md .planning/phases/01-layout-proposal/01-command-contract.md .planning/phases/01-layout-proposal/01-diagnostic-model.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-layout-proposal/01-implementation-gate.md` contains every exact checklist sentence listed in the action text.
    - `.planning/phases/01-layout-proposal/01-implementation-gate.md` contains the exact sentences `Open questions: none.` and `Phase 2 may begin only when every checklist item is Pass.`
    - `.planning/proposals/layout-resolution.md`, `.planning/phases/01-layout-proposal/01-command-contract.md`, and `.planning/phases/01-layout-proposal/01-diagnostic-model.md` all contain `changes doctor --migration-prompt` and do not introduce a separate `layout` command family.
    - `.planning/phases/01-layout-proposal/01-diagnostic-model.md` still contains the exact candidate states `authoritative`, `ambiguous`, `legacy-detected`, and `uninitialized`.
    - None of the reconciled artifacts contain the phrases `choose silently`, `future enhancement`, `placeholder`, or `v1`.
  </acceptance_criteria>
  <done>The proposal bundle has been consistency-checked, the implementation gate says there are no open questions, and Phase 2 can consume the Phase 1 artifacts without revisiting design choices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roadmap requirements → proposal artifacts | Missing or inconsistent traceability can let Phase 2 implement the wrong command or precedence behavior. |
| approved proposal → implementation gate | If the gate checklist differs from the proposal bundle, implementation can drift even when it appears compliant. |
| `doctor --migration-prompt` contract → external operator workflow | Unsafe prompt wording could encourage destructive migration or dual-authority outcomes outside the CLI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | R | `01-requirement-decision-matrix.md` | mitigate | Map every `CMD-01` through `CMD-05` and `D-01` through `D-24` to a concrete artifact and exact section, with no partial statuses. |
| T-02-02 | T | `01-implementation-gate.md` | mitigate | Restate the exact command, precedence, manifest, and safety rules as a pass/fail gate and reconcile the proposal files to match those exact values. |
| T-02-03 | E | Migration brief guidance | mitigate | Require the gate and diagnostic model to preserve advisory-only Markdown output with required verification and explicit no-dual-write instructions. |
| T-02-04 | D | Legacy layout inspection behavior | mitigate | Keep the gate sentence `Legacy-only detection is doctor-visible but invalid for ordinary commands.` and ensure the proposal bundle repeats it verbatim. |
</threat_model>

<verification>
- Run `rg -n "CMD-01|CMD-05|D-01|D-24|Deferred ideas: none\\.|Open design questions: none\\.|Phase 2 entry gate|no-dual-write" .planning/phases/01-layout-proposal/01-requirement-decision-matrix.md .planning/phases/01-layout-proposal/01-implementation-gate.md`.
- Run `rg -n "changes init global|changes doctor --migration-prompt|authoritative|ambiguous|legacy-detected|uninitialized|\\[repo.init\\]|/\\.changes/state/" .planning/proposals/layout-resolution.md .planning/phases/01-layout-proposal/01-command-contract.md .planning/phases/01-layout-proposal/01-diagnostic-model.md`.
- Run `go test ./...`.
</verification>

<success_criteria>
- All tasks completed.
- Every Phase 1 requirement and locked decision has one documented home in the proposal bundle.
- The implementation gate states there are no open questions and no artifact reintroduces silent selection, dual-write behavior, or alternate command naming.
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-proposal/01-02-SUMMARY.md`
</output>
