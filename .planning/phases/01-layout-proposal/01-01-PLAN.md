---
phase: 01-layout-proposal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/proposals/layout-resolution.md
  - .planning/phases/01-layout-proposal/01-command-contract.md
  - .planning/phases/01-layout-proposal/01-diagnostic-model.md
autonomous: true
requirements:
  - CMD-03
  - CMD-04
  - CMD-05
must_haves:
  truths:
    - An operator can read one governing proposal that defines the only supported layout styles as `xdg` and `home` for `global` and `repo` scopes.
    - An operator can predict bootstrap precedence, candidate validity, and ambiguity behavior without reading code.
    - The exact command shapes for `changes init`, `changes init global`, and tiered `changes doctor` output are written down before Phase 2 starts.
  artifacts:
    - path: .planning/proposals/layout-resolution.md
      provides: Governing proposal for supported styles, precedence, manifests, config shape, authority rules, and repo hygiene
      contains: "## Bootstrap and Default Precedence"
    - path: .planning/phases/01-layout-proposal/01-command-contract.md
      provides: Exact command grammar, flag rules, examples, and invalid combinations
      contains: "changes init global [--layout xdg|home] [--home PATH]"
    - path: .planning/phases/01-layout-proposal/01-diagnostic-model.md
      provides: Doctor tier model, candidate states, JSON contract, and migration prompt sections
      contains: "changes doctor --migration-prompt --scope global|repo --to xdg|home [--home PATH] [--output PATH]"
  key_links:
    - from: .planning/proposals/layout-resolution.md
      to: .planning/phases/01-layout-proposal/01-command-contract.md
      via: Approved command surface must use the exact same `init` and `init global` syntax in both files
      pattern: "changes init \\[--layout xdg\\|home\\] \\[--home PATH\\]"
    - from: .planning/proposals/layout-resolution.md
      to: .planning/phases/01-layout-proposal/01-diagnostic-model.md
      via: Doctor and migration sections must restate the same scope, style, authority, and no-dual-write rules
      pattern: "changes doctor --migration-prompt"
---

<objective>
Lock the Phase 1 proposal bundle that future implementation work will treat as the governing design.

Purpose: Convert the locked discussion decisions into explicit proposal artifacts that remove ambiguity around precedence, manifests, config shape, repo hygiene, and command UX.
Output: An updated governing proposal plus phase-local command and diagnostic contracts in `.planning/phases/01-layout-proposal/`.
</objective>

<execution_context>
@/Users/tim.shadel/.local/share/codex/get-shit-done/workflows/execute-plan.md
@/Users/tim.shadel/.local/share/codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-layout-proposal/01-CONTEXT.md
@.planning/proposals/layout-resolution.md
@docs/decisions/ADR-0001-repo-local-xdg-layout.md
@internal/config/config.go
@internal/app/init.go
@internal/cli/app.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the governing layout proposal</name>
  <files>.planning/proposals/layout-resolution.md</files>
  <read_first>.planning/proposals/layout-resolution.md, .planning/phases/01-layout-proposal/01-CONTEXT.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, docs/decisions/ADR-0001-repo-local-xdg-layout.md, internal/config/config.go, internal/app/init.go, internal/cli/app.go</read_first>
  <action>Update `.planning/proposals/layout-resolution.md` so the proposal text explicitly locks the following values without deferring to later phases: supported styles are `xdg` and `home` only for both `global` and `repo` scopes per D-01, D-02, and D-03; global bootstrap precedence is `explicit command flags`, `CHANGES_HOME`, `XDG env vars`, then `built-in default locations` per D-04; repo init precedence is `explicit command flags`, `global config repo-init defaults`, `CHANGES_HOME` as a style preference signal, `XDG env vars` as a style preference signal, then `built-in default locations` per D-05; `CHANGES_HOME` never silently beats a conflicting valid on-disk candidate per D-06. In the manifest and authority sections, state that manifests are structural, symbolic, and low-churn per D-07 and D-08; a candidate is operationally valid only when `layout.toml` parses and matches the candidate `scope` and `style` per D-09; legacy layouts without a manifest are detectable but not operationally valid per D-10; a legacy candidate is diagnosable only if it matches a supported layout shape and contains an authoritative `changes` artifact, with `config.toml` called out as sufficient per D-11; multiple supported candidates are always an ambiguity error per D-12; ordinary commands fail when only legacy-detected layouts exist and only `changes doctor` may inspect those states per D-13; manifest stamping or repair is explicit only per D-14. Keep the exact wording `built-in default locations`, not `built-in XDG defaults`. Keep the manifest examples under `[layout]` with symbolic references `$REPO_ROOT`, `$CHANGES_HOME`, and `$layout.root`; include repo `xdg` and repo `home` examples with `schema_version = 1`, `scope = "repo"`, and `style = "xdg"` or `style = "home"`; and restate the repo `.gitignore` rules exactly as `/.local/state/` for repo `xdg` and `/.changes/state/` for repo `home` per D-24. In the global config section, allow only `[repo.init]`, `style = "xdg"` or `style = "home"`, `home` valid only with `style = "home"`, and `.changes` as the default when `style = "home"` and `home` is omitted per D-22 and D-23. Keep the doctor and migration portions aligned with D-16 through D-21: default `doctor` output stays concise, richer candidate analysis goes behind `--explain`, `--json` remains structured inspection output, migration help is an LLM-oriented structured brief, and the proposal explicitly prohibits dual-write outcomes.</action>
  <verify>rg -n "Global bootstrap when no global layout exists|Repo initialization when no repo layout exists|layout.toml|\\[repo.init\\]|/\\.local/state/|/\\.changes/state/|built-in default locations|legacy-detected|dual-write" .planning/proposals/layout-resolution.md</verify>
  <acceptance_criteria>
    - `.planning/proposals/layout-resolution.md` contains the exact phrase `built-in default locations`.
    - `.planning/proposals/layout-resolution.md` contains `Global bootstrap when no global layout exists` followed by the ordered list `Explicit command flags`, `CHANGES_HOME`, `XDG env vars`, `Built-in default locations`.
    - `.planning/proposals/layout-resolution.md` contains `Repo initialization when no repo layout exists` and the ordered list `Global config repo-init defaults`, `CHANGES_HOME as a style preference signal`, `XDG env vars as a style preference signal`.
    - `.planning/proposals/layout-resolution.md` contains `Layout Manifest Schema`, `layout.toml`, `scope = "repo"`, `style = "xdg"`, and `style = "home"`.
    - `.planning/proposals/layout-resolution.md` contains `[repo.init]`, `style = "home"`, `home = ".changes"`, `/.local/state/`, and `/.changes/state/`.
    - `.planning/proposals/layout-resolution.md` contains statements that `doctor` owns inspection and that migration help prohibits `dual-write` outcomes.
  </acceptance_criteria>
  <done>The governing proposal can stand on its own as the source of truth for precedence, candidate validity, manifests, config shape, repo hygiene, and the non-implementation behavior of `doctor`.</done>
</task>

<task type="auto">
  <name>Task 2: Write the command and diagnostic contracts</name>
  <files>.planning/phases/01-layout-proposal/01-command-contract.md, .planning/phases/01-layout-proposal/01-diagnostic-model.md</files>
  <read_first>.planning/phases/01-layout-proposal/01-command-contract.md, .planning/phases/01-layout-proposal/01-diagnostic-model.md, .planning/proposals/layout-resolution.md, .planning/phases/01-layout-proposal/01-CONTEXT.md, .planning/REQUIREMENTS.md, internal/cli/app.go, internal/app/init.go, internal/config/config.go</read_first>
  <action>Create `.planning/phases/01-layout-proposal/01-command-contract.md` with the title `# Phase 1 Command Contract` and document these exact command lines: `changes init [--layout xdg|home] [--home PATH]`, `changes init global [--layout xdg|home] [--home PATH]`, `changes doctor [--scope global|repo|all] [--explain] [--json]`, and `changes doctor --migration-prompt --scope global|repo --to xdg|home [--home PATH] [--output PATH]`. In that file, document the exact validation rules per D-15 through D-18 and D-22 through D-24: `changes init` is repo-local only, `changes init global` is global only, `--home` is valid only with `--layout home`, repo init defaults may come only from `[repo.init]`, default `doctor` output stays concise, `--explain` is the richer human-oriented tier, `--json` is structured inspection output, and repo-local state ignore rules are `/.local/state/` for `xdg` and `/.changes/state/` for `home`. Create `.planning/phases/01-layout-proposal/01-diagnostic-model.md` with sections `# Phase 1 Diagnostic Model`, `## Candidate States`, `## Default Output`, `## Explain Output`, `## JSON Shape`, and `## Migration Prompt Sections`. In that file, lock the candidate states to `authoritative`, `ambiguous`, `legacy-detected`, and `uninitialized`; define a concrete JSON contract with top-level `requested_scope`, `generated_at`, `global`, `repo`, and `summary` keys, and per-scope fields `status`, `selected_style`, `selected_root`, `precedence_inputs`, `candidates`, and `repair_hint`; and define the migration brief headings exactly as `## Requested Migration`, `## Origin Layout`, `## Destination Layout`, `## Artifact Inventory`, `## Ambiguity and Conflict Notes`, `## Required Verification`, and `## Safety Rules`. In the safety rules section, include the exact instructions `Preserve exactly one authoritative destination.` and `Do not dual-write or keep two live authoritative layouts.` Use the proposal wording, not new command names or a separate `layout` namespace.</action>
  <verify>rg -n "^# Phase 1 Command Contract|changes init global \\[--layout xdg\\|home\\] \\[--home PATH\\]|changes doctor \\[--scope global\\|repo\\|all\\] \\[--explain\\] \\[--json\\]|changes doctor --migration-prompt --scope global\\|repo --to xdg\\|home \\[--home PATH\\] \\[--output PATH\\]" .planning/phases/01-layout-proposal/01-command-contract.md && rg -n "^# Phase 1 Diagnostic Model|authoritative|ambiguous|legacy-detected|uninitialized|requested_scope|generated_at|## Required Verification|Preserve exactly one authoritative destination\\.|Do not dual-write or keep two live authoritative layouts\\." .planning/phases/01-layout-proposal/01-diagnostic-model.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-layout-proposal/01-command-contract.md` contains all four exact command lines documented in the action text.
    - `.planning/phases/01-layout-proposal/01-command-contract.md` contains the exact phrases `--home is valid only with --layout home`, `changes init is repo-local only`, and `changes init global is global only`.
    - `.planning/phases/01-layout-proposal/01-diagnostic-model.md` contains the exact candidate states `authoritative`, `ambiguous`, `legacy-detected`, and `uninitialized`.
    - `.planning/phases/01-layout-proposal/01-diagnostic-model.md` contains the exact JSON keys `requested_scope`, `generated_at`, `global`, `repo`, `summary`, `status`, `selected_style`, `selected_root`, `precedence_inputs`, `candidates`, and `repair_hint`.
    - `.planning/phases/01-layout-proposal/01-diagnostic-model.md` contains the exact section headings `## Requested Migration`, `## Origin Layout`, `## Destination Layout`, `## Artifact Inventory`, `## Ambiguity and Conflict Notes`, `## Required Verification`, and `## Safety Rules`.
    - `.planning/phases/01-layout-proposal/01-diagnostic-model.md` contains the exact sentences `Preserve exactly one authoritative destination.` and `Do not dual-write or keep two live authoritative layouts.`
  </acceptance_criteria>
  <done>The phase directory contains stable command and diagnostic contracts that future implementation and review work can reference without reopening command naming, flag validation, or doctor output shape debates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator intent → CLI contract | Untrusted flags and scope selections must be documented clearly enough that implementation does not guess at invalid combinations. |
| environment and filesystem → authority rules | `CHANGES_HOME`, XDG env vars, manifests, and discovered directories can conflict; the proposal must describe how ambiguity is detected instead of silently resolved. |
| `changes doctor` output → external migration tool | The migration brief leaves the program boundary and must not imply destructive or dual-write behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `.planning/proposals/layout-resolution.md` precedence sections | mitigate | Document the exact precedence orders from D-04 and D-05 and state that `CHANGES_HOME` does not silently override a conflicting valid on-disk layout. |
| T-01-02 | T | Manifest validity rules | mitigate | Require `layout.toml` to parse and match candidate `scope` and `style`, and document that legacy-only layouts are diagnosable but not operationally valid. |
| T-01-03 | I | `01-diagnostic-model.md` migration brief | mitigate | Lock the safety rules to advisory Markdown output with explicit `Preserve exactly one authoritative destination.` and `Do not dual-write...` instructions. |
| T-01-04 | D | `01-command-contract.md` doctor UX | mitigate | Keep default `doctor` output concise and move richer candidate details to `--explain` so diagnostic behavior is explicit and bounded. |
</threat_model>

<verification>
- Run `rg -n "built-in default locations|\\[repo.init\\]|changes init global|changes doctor --migration-prompt|authoritative|legacy-detected|Do not dual-write" .planning/proposals/layout-resolution.md .planning/phases/01-layout-proposal/01-command-contract.md .planning/phases/01-layout-proposal/01-diagnostic-model.md`.
- Run `go test ./...`.
</verification>

<success_criteria>
- All tasks completed.
- The governing proposal and both phase-local contract documents exist and use the exact command names, precedence order, and safety rules from the locked decisions.
- No artifact introduces a third layout style, a separate `layout` command family, or permissive dual-write language.
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-proposal/01-01-SUMMARY.md`
</output>
